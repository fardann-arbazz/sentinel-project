[profile.default]
src = "src"
out = "out"
libs = ["lib"]
gas_price = 3000000000  # 3 gwei

[rpc_endpoints]
bsc_testnet = "${BSC_TESTNET_RPC}"

[etherscan]
bsc_testnet_api = { key = "${ETHERSCAN_API_KEY}", chain = 97 }